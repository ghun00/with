// AI 원문 입력 권장 상한(글자 수). 2026-07-24부로 생성 버튼을 막지는 않는 단순 안내
// 문구 기준값이다 — 실제 하드 캡은 서버 MAX_RAW_TEXT_LENGTH(생성+검증 2패스가 Edge
// Function 150초 wall-clock 한도 안에 들도록 하는 가드)이며, 이 값은 서버 호출 전에
// 걸러지므로 546 없이 바로 안내 에러로 응답한다. 두 값을 반드시 함께 맞출 것.
// index.ts↔real.ts 순환 참조를 피하려고 의존성 없는 별도 모듈에 둔다.
// 2026-07-21: 12,000 → 30,000 → 40,000으로 임시 확대. 2026-07-24: 40,000 → 50,000으로 확대.
export const MAX_AI_SOURCE_LENGTH = 50000
